export {
  CONSTANT_TAG,
  CURRENT_TAG,
  consumeTag,
  createCache,
  dirtyTagFor,
  getValue,
  tagFor,
  tagMetaFor,
  untrack,
  validateTag,
  valueForTag,
  isTracking,
  updateTag,
  isConst,
  track,
  trackedData,
} from '@glimmer/validator';
export type { Cache, Tag, TagMeta, UpdatableTag } from '@glimmer/validator';
